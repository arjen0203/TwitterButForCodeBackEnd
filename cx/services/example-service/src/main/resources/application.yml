spring:
  applicaton:
    name: example-service
  cloud:
    kubernetes:
      discovery:
        all-namespaces: true
        service-labels:
          spring-boot: true
server:
  servlet:
    context-path: "/example"